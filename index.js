/**
 * dList
 * 
 * @author David Gaspar
 * @format
 */

import { AppRegistry } from 'react-native';
import { app as runApp } from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, runApp);
